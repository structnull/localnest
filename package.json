{
  "name": "localnest",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun run start-server & bun run start-client",
    "start-api": "bun --cwd server start",
    "start-client": "bun --cwd client dev"
  }
}

